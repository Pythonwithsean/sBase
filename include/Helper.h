#ifndef Helper_H
#define Helper_H
#include <string>
#include <iostream>
#include "Tokenizer.h"

std ::string toLowerCase(const std::string &str);
std ::string convertTokenTypeToString(TokenType type);

#endif // Helper_H